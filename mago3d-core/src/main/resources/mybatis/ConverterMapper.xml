<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gaia3d.persistence.ConverterMapper">

	<!-- converter job 총 건수 -->
	<select id="getListConverterJobTotalCount" parameterType="converterJob" resultType="long">
		/* getListConverterJobTotalCount */
		SELECT COUNT(converter_job_id) 
		FROM converter_job
		WHERE user_id = #{user_id}
			<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '0'.toString()">
			AND ${search_word} = #{search_value}
			</if>
			<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '1'.toString()">
			AND ${search_word} LIKE '%' || #{search_value} || '%'
			</if>
			<if test="status != null and status != ''">
			AND status = #{status}
			</if>
			<if test="start_date != null and start_date != ''">
			<![CDATA[
			AND insert_date >= TO_TIMESTAMP(#{start_date}, 'YYYYMMDDHH24MISSUS')
			]]>
			</if>
			<if test="end_date != null and end_date != ''">
			<![CDATA[
			AND insert_date <= TO_TIMESTAMP(#{end_date}, 'YYYYMMDDHH24MISSUS')
			]]>
			</if>
	</select>
	
	<!-- converter job file 총 건수 -->
	<select id="getListConverterJobFileTotalCount" parameterType="converterJobFile" resultType="long">
		/* getListConverterJobFileTotalCount */
		SELECT COUNT(converter_job_file_id) 
		FROM converter_job_file
		WHERE user_id = #{user_id}
			<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '0'.toString()">
			AND ${search_word} = #{search_value}
			</if>
			<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '1'.toString()">
			AND ${search_word} LIKE '%' || #{search_value} || '%'
			</if>
			<if test="status != null and status != ''">
			AND status = #{status}
			</if>
			<if test="start_date != null and start_date != ''">
			<![CDATA[
			AND insert_date >= TO_TIMESTAMP(#{start_date}, 'YYYYMMDDHH24MISSUS')
			]]>
			</if>
			<if test="end_date != null and end_date != ''">
			<![CDATA[
			AND insert_date <= TO_TIMESTAMP(#{end_date}, 'YYYYMMDDHH24MISSUS')
			]]>
			</if>
	</select>
	
	<!-- converter job 목록 -->
	<select id="getListConverterJob" parameterType="converterJob" resultType="converterJob">
		/* getListConverterJob */
		SELECT X.*, 
			(SELECT COUNT(converter_job_file_id) FROM converter_job_file WHERE converter_job_id = X.converter_job_id) AS converter_file_count
		FROM (
			SELECT A.*
			FROM converter_job A
			WHERE A.user_id = #{user_id}
				<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '0'.toString()">
				AND A.${search_word} = #{search_value}
				</if>
				<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '1'.toString()">
				AND A.${search_word} LIKE '%' || #{search_value} || '%'
				</if>
				<if test="status != null and status != ''">
				AND A.status = #{status}
				</if>
				<if test="start_date != null and start_date != ''">
				<![CDATA[
				AND A.insert_date >= TO_TIMESTAMP(#{start_date}, 'YYYYMMDDHH24MISSUS')
				]]>
				</if>
				<if test="end_date != null and end_date != ''">
				<![CDATA[
				AND A.insert_date <= TO_TIMESTAMP(#{end_date}, 'YYYYMMDDHH24MISSUS')
				]]>
				</if>
			<choose>
			<when test="order_word != null and order_word != '' and order_value != null and order_value != ''">
			ORDER BY A.${order_word} ${order_value}
			</when>
			<otherwise>
			ORDER BY A.insert_date DESC, A.converter_job_id DESC
			</otherwise>
			</choose>
			OFFSET #{offset} LIMIT #{limit}
		) X
	</select>
	
	<!-- converter job file 목록 -->
	<select id="getListConverterJobFile" parameterType="converterJobFile" resultType="converterJobFile">
		/* getListConverterJobFile */
		SELECT X.*
		FROM (
			SELECT A.*, B.sharing_type, B.data_type, B.file_name
			FROM converter_job_file A, upload_data_file B
			WHERE A.user_id = #{user_id}
				AND A.upload_data_file_id = B.upload_data_file_id
				<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '0'.toString()">
				AND B.${search_word} = #{search_value}
				</if>
				<if test="search_word != null and search_word != '' and search_value != null and search_value != '' and search_option == '1'.toString()">
				AND B.${search_word} LIKE '%' || #{search_value} || '%'
				</if>
				<if test="status != null and status != ''">
				AND A.status = #{status}
				</if>
				<if test="start_date != null and start_date != ''">
				<![CDATA[
				AND A.insert_date >= TO_TIMESTAMP(#{start_date}, 'YYYYMMDDHH24MISSUS')
				]]>
				</if>
				<if test="end_date != null and end_date != ''">
				<![CDATA[
				AND A.insert_date <= TO_TIMESTAMP(#{end_date}, 'YYYYMMDDHH24MISSUS')
				]]>
				</if>
			<choose>
			<when test="order_word != null and order_word != '' and order_value != null and order_value != ''">
			ORDER BY A.${order_word} ${order_value}
			</when>
			<otherwise>
			ORDER BY A.insert_date DESC, A.converter_job_id DESC
			</otherwise>
			</choose>
			OFFSET #{offset} LIMIT #{limit}
		) X
	</select>
	
	<!-- converter job 등록 -->
	<insert id="insertConverterJob" parameterType="converterJob">
		/* insertConverterJob */
		<selectKey keyProperty="converter_job_id" resultType="long" order="BEFORE">
    		SELECT NEXTVAL('converter_job_seq')
  		</selectKey>
		INSERT INTO converter_job(
			converter_job_id, upload_data_id, user_id, title, converter_type, file_count,
			<if test="status != null and status != ''">
			status, 
			</if>
			error_code
		) VALUES (
			#{converter_job_id}, #{upload_data_id}, #{user_id}, #{title}, #{converter_type}, #{file_count},
			<if test="status != null and status != ''">
			#{status}, 
			</if>
			#{error_code}
		)
	</insert>
	
	<!-- converter job file 등록 -->
	<insert id="insertConverterJobFile" parameterType="converterJobFile">
		/* insertConverterJobFile */
		<selectKey keyProperty="converter_job_file_id" resultType="long" order="BEFORE">
    		SELECT NEXTVAL('converter_job_file_seq')
  		</selectKey>
		INSERT INTO converter_job_file(
			converter_job_file_id, converter_job_id, upload_data_id, upload_data_file_id, project_id, user_id,
			<if test="status != null and status != ''">
			status, 
			</if>
			error_code
		) VALUES (
			#{converter_job_file_id}, #{converter_job_id}, #{upload_data_id}, #{upload_data_file_id}, #{project_id}, #{user_id},
			<if test="status != null and status != ''">
			#{status}, 
			</if>
			#{error_code}
		)
	</insert>
	
	<!-- converter job -->	
	<update id="updateConverterJob" parameterType="converterJob">
		/* updateConverterJob */
		UPDATE converter_job
		SET 
			<if test="title != null and title != ''">
			title = #{title},
			</if>
			<if test="status != null and status != ''">
			status = #{status},
			</if>
			<if test="converter_type != null and converter_type != ''">
			converter_type = #{converter_type},
			</if>
			<if test="error_code != null and error_code != ''">
			error_code = #{error_code},
			</if>
			update_date = now()
		WHERE converter_job_id = #{converter_job_id}
			<if test="user_id != null and user_id != ''">
			AND user_id = #{user_id}
			</if>
	</update>
</mapper>