<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gaia3d.persistence.AttributeRepositoryMapper">

	<!-- converter job 총 건수 -->
	<select id="getDataAttribute" parameterType="string" resultType="attributeRepository" >
		/* getDataAttribute */
		SELECT * 
		FROM mj_build 
		WHERE build_name = #{buildName}
	</select>
</mapper>